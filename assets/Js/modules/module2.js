/**
 * Fonction qui retourne un message d'adieu.
 * @param {string} name - Le nom de la personne à qui dire au revoir.
 * @returns {string} - Message d'adieu.
 */
export function farewell(name) {
    return `Goodbye, ${name}!`;
}